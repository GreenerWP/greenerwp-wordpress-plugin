/**
 * Implements the image preview link.
 */
class ImagePreviewLink {
  constructor( args ) {
		this.args = args;
		this.render();
		console.log( 'hey' );
	}
	render() {
	};
};

export default ImagePreviewLink;